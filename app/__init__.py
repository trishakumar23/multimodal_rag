"""Multimodal report ingestion application.

Flow: main → extraction → chunking → images → database; debug displays saved results.
Configuration defaults and environment overrides live in config.
"""
